services:
  go-cli-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: go-cli-test:latest
    container_name: go-cli-test
    # Override the default command if needed
    # command: ["greet", "--name=Docker"]

    # If your CLI needs to read files, you can mount volumes
    # volumes:
    #   - ./data:/app/data:ro

    # Environment variables (if needed)
    # environment:
    #   - ENV_VAR=value

    # Remove container after it exits
    restart: "no"
